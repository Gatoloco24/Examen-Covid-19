<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.inacap.examencovid_19">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExamenCovid19">
        <activity android:name=".PacienteListView"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".toolbar3" />
        <activity android:name=".toolbar2" />
        <activity android:name=".toolbar" />
        <activity android:name=".VerPaciente" />
        <activity android:name=".RegistroPaciente" />
        <activity
            android:name=".Inicio"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExamenCovid19.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>